# dashboards/dashboards.yaml

#NOTE: changes in this file require restart, but changes in dashboards
#      can be refreshed with the UI triple-dot menu option "Refresh"

# home-view:
#   mode: yaml
#   title: Home
#   icon: mdi:home
#   show_in_sidebar: true
#   require_admin: false
# #   #note: this path is relative to config folder, not folder where this file is
#   filename: dash-home-v2.yaml
dash-room-by-room:
  mode: yaml
  title: Groups
  icon: mdi:floor-plan
  show_in_sidebar: true
  require_admin: False
  filename: dash-room-by-room.yaml
dash-room-details:
  mode: yaml
  title: Details
  icon: mdi:list-status
  show_in_sidebar: true
  require_admin: False
  filename: dash-room-details.yaml
# dash-shawna:
#   mode: yaml
#   title: Shawna's Home
#   icon: mdi:home-circle
#   show_in_sidebar: true
#   require_admin: false
#   filename: dash-shawna.yaml
