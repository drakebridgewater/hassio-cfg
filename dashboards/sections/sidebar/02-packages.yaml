type: vertical-stack
cards: 
  - type: custom:bubble-card
    card_type: button
    modules:
      - default
    button_type: state
    entity: binary_sensor.deck_package_occupancy
    show_last_changed: true
    name: Package Delivered
    icon: mdi:package-check
    show_last_updated: false
    show_attribute: false

visibility:
  - condition: state
    entity: binary_sensor.deck_package_occupancy
    state: "on"