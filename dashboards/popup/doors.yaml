type: vertical-stack
cards:
- type: custom:bubble-card
  card_type: pop-up
  hash: '#door'
  button_type: name
  name: Door Sensors
- type: custom:auto-entities
  card_param: cards
  card:
    type: grid
    columns: 1
    square: false
  filter:
    include:
      - domain: binary_sensor
        attributes:
          device_class: door
        options:
          type: custom:bubble-card
          card_type: button
          button_type: state
    exclude: 
      - state: unavailable
      - state: unknown
  sort:
    method: state
    reverse: true